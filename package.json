{
  "name": "spoketome",
  "version": "0.1.0",
  "description": "Pull Notion pages as Markdown via .spoketome files",
  "type": "module",
  "bin": {
    "spoketome": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "prepublishOnly": "npm run build",
    "demo": "tsx src/index.ts --dir demo --verbose"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "dependencies": {
    "@notionhq/client": "^2.2.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "tsup": "^8.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.4.0"
  }
}
